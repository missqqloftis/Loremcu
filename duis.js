switch (expression) {
  case "MultiPoint":
    // Statements to execute if expression === "MultiPoint"
    break;
  case "LineString":
    // Statements to execute if expression === "LineString"
    break;
  default:
    // Statements to execute if expression doesn't match any case
}
