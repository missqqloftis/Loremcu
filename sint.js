// Define the wrapper object
let wrapper = {};

// Define the version
let version = "1.0.0";

// Set the version property on the wrapper object
wrapper.version = version;

console.log(wrapper.version); // Output: "1.0.0"
